fn main() {
    println!("Hello, world!");

    // Create values 1 to 3 inclusive
    let range = 'a'..='z';
    dbg!(range);

    // Create values 1 to 4 not-inclusive
    let range2 = 1..4;
    dbg!(range2);
}
