#[allow(unused_variables)]

fn main() {
    println!("Hello, world!");

    // Annotate that the variable type is int32
    let num: i32 = 14;
    let a: char = 'a';

    // Generics
    let numbers: Vec<i32> = vec![1, 2, 3];
}
